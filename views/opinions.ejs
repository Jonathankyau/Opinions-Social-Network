
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Opinion</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1 class="text-center text-6xl text-purple-700">Opinions</h1>
    <ul class="text-center text-black-700">
    <% opinions.forEach( el => { %>
            <li class='opinionItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.opinion %></span>
                <span class='del'> Delete </span>
            </li>
    <% }) %>    
    </ul>

    <h2 class="text-center"><%= user.userName %> has <%= left %> Opinions.</h2>

    <form class="text-center" action="/opinions/createOpinion" method='POST'>
        <input class="border rounded-lg" type="text" placeholder="Enter Opinion" name='opinionItem'>
        <input class="h-6 px-5 m-2 text-indigo-100 transition-colors duration-150 bg-indigo-700 rounded-lg focus:shadow-outline hover:bg-indigo-800" type="submit">
    </form>

    <button class="h-6 px-5 m-2 text-indigo-100 transition-colors duration-150 bg-indigo-700 rounded-lg focus:shadow-outline hover:bg-indigo-800" href="/logout">Logout</button>

    <a class="text-center" href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>